<UserControl x:Class="Kulagin.Mastering_WPF.Views.BitRateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:Enums="clr-namespace:Kulagin.Mastering_WPF.DataModels.Enums;assembly=Kulagin.MasteringWPF.DataModels"
             xmlns:Converters="clr-namespace:Kulagin.Mastering_WPF.Converters;assembly=Kulagin.MasteringWPF.Converters">
    <UserControl.Resources>
        <Converters:EnumToBoolConverter x:Key="EnumToBoolConverter" />
        <Converters:EnumToDescriptionStringConverter x:Key="EnumToDescriptionStringConverter" />
    </UserControl.Resources>
    <GroupBox Header="Audio Quality" FontSize="14" Margin="20" HorizontalAlignment="Left" VerticalAlignment="Top" Padding="5">
        <ListBox ItemsSource="{Binding BitRates}" SelectedItem="{Binding BitRate}">
            <ListBox.ItemTemplate>
                <DataTemplate DataType="{x:Type Enums:BitRate}">
                    <RadioButton Content="{Binding ., Converter={StaticResource EnumToDescriptionStringConverter}}"
                                 IsChecked="{Binding IsSelected, RelativeSource={RelativeSource AncestorType={x:Type ListBoxItem}}, FallbackValue=False}"
                                 VerticalContentAlignment="Center" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </GroupBox>
</UserControl>
